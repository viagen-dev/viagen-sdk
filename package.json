{
  "name": "viagen-sdk-root",
  "private": true,
  "workspaces": [
    "api",
    "sdk",
    "ui"
  ],
  "scripts": {
    "dev": "concurrently -n api,ui -c blue,magenta \"npm run dev -w api\" \"npm run dev -w ui\"",
    "test": "npm test --workspaces",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
